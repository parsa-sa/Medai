
  # Enable swipe right carousel

  This is a code bundle for Enable swipe right carousel. The original project is available at https://www.figma.com/design/koeR6hXFHArWaxqELS4qG0/Enable-swipe-right-carousel.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  